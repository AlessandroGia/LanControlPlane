services:
  lan-control-plane-agent:
    build:
      context: ..
      dockerfile: agent/Dockerfile
    container_name: lan-control-plane-agent
    restart: unless-stopped
    env_file:
      - ./.env
    working_dir: /app #uv lock command in root
