services:
  lan-control-plane-agent:
    image: lan-control-plane-agent:local
    build:
      context: ..
      dockerfile: agent/Dockerfile
    container_name: lan-control-plane-agent
    restart: unless-stopped
    env_file:
      - ./.env
    working_dir: /app/agent
